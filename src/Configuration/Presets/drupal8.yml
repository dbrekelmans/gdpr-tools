users_field_data:
  name:
    type:
      name: string
      options:
        minlength: 4
        maxlength: 20
    unique: true
  pass:
    type:
      name: password
      options:
        encryption: sha512
  mail:
    type:
      name: email
    unique: true
  init:
    type:
      name: null
#  init:
#    type:
#      name: duplicate
#      options:
#        column: mail
